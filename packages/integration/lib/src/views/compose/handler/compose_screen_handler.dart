import 'package:likeminds_feed_flutter_core/src/utils/tagging/helpers/tagging_helper.dart';

void checkTextLinks() {
  // String link = getFirstValidLinkFromString(_controller.text);
  // if (link.isEmpty) {
  //   linkModel = null;
  // } else if (linkModel != null && postMedia.isEmpty && showLinkPreview) {
  //   postMedia.add(linkModel!);
  // }
}
